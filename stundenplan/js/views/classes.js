/* STUNDENWERK · views/classes.js — Klassen (Platzhalter, wird ersetzt) */
(function () {
  const SW = (globalThis.SW = globalThis.SW || {});
  SW.views = SW.views || {};
  SW.views['klassen'] = { title: 'Klassen', render(el) { el.append(SW.ui.pageHeader({ title: 'Klassen' }), SW.ui.empty({ icon: '🚧', title: 'In Arbeit', text: 'Diese Ansicht wird gerade gebaut.' })); } };
})();
